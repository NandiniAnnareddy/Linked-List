
class Node:
    def __init__(self, data):
        self.data = data
        self.next = None
        
class Solution:
    def getCount(self, head):
        node = head
        count = 0
        while(node.next !=None):
            count += 1
            node = node.next
        count += 1
        return count
        
